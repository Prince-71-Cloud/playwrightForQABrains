name: Playwright Tests with Allure Reports

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: "Chrome"
            browser: "chromium"
            channel: "chrome"
          - name: "Firefox"
            browser: "firefox"
          - name: "Safari"
            browser: "webkit"

    steps:
    - uses: actions/checkout@v4
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps ${{ matrix.browser }}
    - name: Run Playwright tests
      id: test
      run: npx playwright test --project=${{ matrix.browser }}
      env:
        CI: true
        BASE_URL: ${{ secrets.BASE_URL }}
        BROWSER: ${{ matrix.browser }}
        CHANNEL: ${{ matrix.channel }}
        EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        DB_HOST: ${{ secrets.DB_HOST }}
        DB_PORT: ${{ secrets.DB_PORT }}
        DB_NAME: ${{ secrets.DB_NAME }}
        DB_USER: ${{ secrets.DB_USER }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        ALLURE_RESULTS_DIR: allure-results

    - name: Generate Allure Report (on test failure)
      uses: simple-elf/allure-report-action@v1.6
      if: failure()
      with:
        allure_results: allure-results
        allure_report: allure-report
        allure_history: allure-history

    - name: Archive Allure Report (on test failure)
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: allure-report-failure
        path: allure-report

    - name: Generate Allure Report (on test success)
      uses: simple-elf/allure-report-action@v1.6
      if: success()
      with:
        allure_results: allure-results
        allure_report: allure-report
        allure_history: allure-history

    - name: Archive Allure Report (on test success)
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: allure-report-success
        path: allure-report